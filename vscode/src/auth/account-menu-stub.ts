/**
 * Stub implementation for account menu functionality in standalone mode
 * Authentication is disabled, so this returns immediately without action
 */
export async function showAccountMenu(): Promise<void> {
    // No-op in standalone mode
    return
}
